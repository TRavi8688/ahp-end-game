# Add to backend/auth-service/.env.example
# Add to backend/healthcare-core/.env.example

# SEC-5: Comma-separated list of allowed CORS origins. Required in all environments.
# Never use '*'. Startup will fail if this is empty.
ALLOWED_ORIGINS=REPLACE_ME
# Example: ALLOWED_ORIGINS=https://app.hospyn.in,https://admin.hospyn.in
