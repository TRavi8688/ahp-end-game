# healthcare-core/requirements.txt
# FIXES:
#   - Added python-jose[cryptography] (imported in rbac.py, partner_auth.py — was missing)
#   - Added sentry-sdk[fastapi]
#   - Added opentelemetry packages
#   - Added structlog (imported in main.py)
#   - Pinned all versions for reproducible builds
#
# NOTE: rbac.py now uses PyJWT (see audit BUG-11). python-jose is kept here
# only for partner_auth.py which may still use it — migrate partner_auth.py
# to PyJWT in the next iteration to fully eliminate python-jose.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Auth — RS256 validation (matches auth-service)
PyJWT[crypto]==2.9.0
cryptography==43.0.1
python-jose[cryptography]==3.3.0   # FIX: was missing; needed by partner_auth.py

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.3

# Redis
redis[hiredis]==5.1.0

# HTTP client (service-to-service calls)
httpx==0.27.2

# Validation
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
email-validator==2.2.0

# Structured logging — FIX: was imported in main.py but missing
structlog==24.4.0

# Storage
google-cloud-storage==2.18.2

# Crypto for PHI field encryption
cryptography==43.0.1

# Observability
sentry-sdk[fastapi]==2.16.0
opentelemetry-sdk==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-instrumentation-sqlalchemy==0.48b0
opentelemetry-instrumentation-redis==0.48b0
opentelemetry-instrumentation-httpx==0.48b0
opentelemetry-exporter-otlp-proto-grpc==1.27.0

# Utilities
python-dateutil==2.9.0
phonenumbers==8.13.46
pillow==10.4.0

# WebSocket support (for ws_endpoint.py)
websockets==13.1
